export const DEFAULT_PORT = 4200;
export const DEFAULT_ADMIN_EMAIL = 'admin@involute.local';

export const DEFAULT_WORKFLOW_STATE_ORDER = [
  'Backlog',
  'Ready',
  'In Progress',
  'In Review',
  'Done',
  'Canceled',
] as const;
